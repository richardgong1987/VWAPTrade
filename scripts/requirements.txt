# run_conditions.py 的 Python 依赖
# 安装： pip install -r scripts/requirements.txt

# 解析 scripts/.env 环境配置
python-dotenv>=1.0

# 回测计划来自后端参数接口，用标准库 urllib 请求，无需额外依赖。

# 汇总回测报告 JSON、生成胜率/盈亏柱状图 final_report.png
pandas>=2.0
matplotlib>=3.7
